#!/usr/bin/env node

require('commander')
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('init', '初始化前端项目目录')
  .command('start', '执行QY')
  .command('upgrade', '更新QY工具')
  .parse(process.argv)